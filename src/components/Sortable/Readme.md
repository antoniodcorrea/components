# Sortable

## Requirements

Items must have `data-id` and `data-order` attributes
It requires a HTML element with id "Handle", so we can using to sort items. Can be an element wrapping everything
We need react and react-dom versions to 16.8.0 due to rendering issues with version 17
