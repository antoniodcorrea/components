# Sortable

## Requirements

Items must have `data-id` and `data-order` attributes
It requires a HTML element with id "handle", so we can using to sort items.
The handle can not be the outer element.
