# TextEditor

## Description

Component to create and save rich content texts.
Initialized with initial content via `initialValue` prop in a controlled manner. After initialization component is uncontrolled.
Changes are sent to parent via `prop`.
