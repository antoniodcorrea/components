# SortBy component

- Should it be a controlled component?
- Maybe it shouldnt connect to redux
  The issue is how to store/retrieve the sort value in the store, as the format for the SortBy component doesn't match any sort param
  Maybe use the field/-field format instead of the asc/desc format
