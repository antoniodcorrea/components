import path from 'path';

export const ROUTE_ROOT = path.resolve(process.cwd());

export const ROUTE_BUILD = path.resolve(process.cwd(), 'webpack');

export const ROUTE_DIST = path.resolve(process.cwd(), 'dist');

export const ROUTE_SRC = path.resolve(process.cwd(), 'src');
